# BCP-Vis 状态巡检报告
**时间**：2026-09-12 01:54 (GMT+8) ｜ **连接**：Ed25519 密钥（status_check.py，无需密码）

## 1) GPU 负载
- **未满载 / 实际空闲**：`util=0%`、`mem=1 MiB / 24564 MiB`、无任何 compute 进程。
- 仅 jupyter-lab / tensorboard / watchdog 在跑，无训练负载 → 整条实验链当前**停滞**（下游症状，根因见 E13）。

## 2) 各实验 E11 / E12 / E13 / E14
| 实验 | 状态 | 说明 |
|---|---|---|
| **E11** (V7 altmoe) | ✅ 已完成 | `e11_queue.done` 存在；val IoU = **0.8927** |
| **E12** (V8 级联) | ✅ 已完成 | 01:21 DONE；val IoU = **0.8939**（macro 0.8515 / F1 0.8953），较 E11 略升 |
| **E13** (UAP 第3创新点) | ⚠️ **卡死** | 队列进程存活 (PID 367109) 但停等循环；GPU 空闲、无子进程、01:24 后无新日志；无 `e13_queue.done`、CSV 无 E13 行 → **尚未产出** |
| **E14** (类别消融) | ❌ 主训练失败 | watchdog 01:40 捕获 `e14_queue.log [FAIL] E14 train`；`e14_queue.done` 被脚本无条件写出但 CSV 无 E14 eval 行 → 无有效 val IoU |

### E13 死锁根因（重点）
`run_e13_queue.sh` 实际有**两个**等待循环：先等 `e12_queue.done`，再等 **`logs/e14_parallel.done`**。
而 `e14_parallel.done` **不存在且永远不会自动出现**——`run_e14_parallel.sh`（prior_v5_cat）的训练其实已于 09-11 18:58 完成（产出 `priors_cat`，best dice 0.73243），但**漏写 done 标记**。watchdog 也未将其列为待恢复项（alive=[] pending=[]）。故 E13 的等待循环将永久 sleep，GPU 持续空转。

## 3) 异常核查（ABORT/FAIL/NameError）
- **e13**：日志无 ABORT/FAIL/NameError（干净）。
- **e12**：仅余 9/10 历史 `NameError: class_aware → [FAIL] V8 → [ABORT] ckpt 缺失`，已 RELAUNCHED_OK 修复；当前 E12 成功退出 → 与"重跑前旧迹非当前错误"判断一致。
- **e14**：有**当前真实** `[FAIL] E14 train`（非历史）。
- 提示：e12 旧 ABORT 是重跑前残留，当前 run_e12 已产出有效结果，非当前错误。

## 4) E13（第3创新点 UAP）结果
- **尚未产出**（卡死中），暂无 val IoU。需解除死锁后才自动接续 UAP 先验推理 + E13 训练。

## 5) 已完成实验 val IoU 汇总
E8 = 0.8900 ｜ E9 = 0.8912 ｜ E10 = 0.8918 ｜ E11 = 0.8927 ｜ E12 = 0.8939

## 建议（未执行，待确认）
E13 仅被"缺失的 `e14_parallel.done`"卡住，而并行训练实际已完成。在服务器执行一行即可解锁：
```
touch <PROJECT_ROOT>/logs/e14_parallel.done
```
E13 等待循环（180s 轮询）检测到后即自动接续 UAP。E14 主训练失败是独立问题，不影响 E13 的数据依赖（E13 只用 V8 ckpt）。是否执行请确认。
